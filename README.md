# ROFGCDC Backend API 🚛♻️

نظام خلفي (Backend) متكامل مبني باستخدام **Django** و **Django Rest Framework** لإدارة عمليات جمع النفايات وتحسين المسارات (Route Optimization) في مدينة دمشق.  
يهدف النظام إلى مساعدة البلديات والمخططين في إدارة الحاويات، المركبات، وإنشاء سيناريوهات جمع فعالة لتقليل التكاليف والمسافات المقطوعة.

---

## ✨ الميزات الرئيسية

### 1️⃣ إدارة المستخدمين والمصادقة (Accounts & Users)

- **نظام أدوار (RBAC):**
  - **Admin:** صلاحيات كاملة لإدارة النظام والإحصائيات.
  - **Planner:** إنشاء خطط وسيناريوهات الجمع.
  - **Driver:** تنفيذ المهام.

- **أمان متقدم:**
  - تسجيل الدخول باستخدام **JWT** مخزن في **HttpOnly Cookies**.
  - نظام **OTP** عبر البريد الإلكتروني لتفعيل الحسابات واستعادة كلمة المرور.
  - أرشفة المستخدمين (Soft Delete)، تفعيل/تعطيل الحسابات، وسجل النشاطات.

---

### 2️⃣ خوارزميات التحسين (Optimization)

- حل مشكلة توجيه المركبات (**VRP**) باستخدام **Google OR-Tools**.
- حساب المسافات والزمن الفعلي عبر **OSRM**.
- إنشاء سيناريوهات جمع مخصصة حسب:
  - التاريخ
  - البلدية
  - المركبات المتاحة

---

### 3️⃣ إدارة الأصول (Assets Management)

- **الحاويات (Bins):** المواقع، السعات.
- **المركبات (Vehicles):** السعة، نقطة الانطلاق.
- **البلديات والمطامر.**
- **تحقق جغرافي:** ضمان أن جميع الإحداثيات ضمن حدود مدينة دمشق.

---

## 🛠️ التقنيات المستخدمة

- Python 3.x  
- Django 5.2 & Django Rest Framework  
- PostgreSQL  
- Google OR-Tools  
- OSRM (يعمل محلياً)  
- JWT Authentication  
- SMTP (إرسال OTP)

---

## 🚀 التثبيت والتشغيل

### المتطلبات المسبقة

- Python 3.10+
- PostgreSQL
- OSRM يعمل محلياً على المنفذ `5000`

---

### خطوات التثبيت

#### 1. استنساخ المستودع
```bash
git clone <repository-url>
cd ROFGCDC_Backend
```

#### 2. إنشاء وتفعيل بيئة افتراضية

**Windows**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux / Mac**
```bash
python3 -m venv venv
source venv/bin/activate
```

#### 3. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

#### 4. إعداد ملف البيئة `.env`
أنشئ ملف `.env` بجانب `manage.py`:

```env
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
```

#### 5. تنفيذ التهجيرات
```bash
python manage.py migrate
```

#### 6. إنشاء مستخدم مسؤول
```bash
python manage.py createsuperuser
```

#### 7. تشغيل الخادم
```bash
python manage.py runserver
```

---

## 📡 تشغيل OSRM

```bash
docker run -t -i -p 5000:5000 -v "${PWD}:/data" osrm/osrm-backend \
osrm-routed --algorithm mld /data/syria-latest.osrm
```

---
